<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="HomeGlass.Pages.RoomDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="28" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock x:Name="RoomNameTextBlock" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="Room details" />
        </StackPanel>

        <ItemsControl x:Name="StatusItemsControl" Grid.Row="1">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border
                        Margin="0,0,8,8"
                        Padding="10,6"
                        Background="{Binding Background}"
                        CornerRadius="8"
                        ToolTipService.ToolTip="{Binding ToolTip}">
                        <TextBlock FontWeight="SemiBold" Foreground="{Binding Foreground}" Text="{Binding Text}" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ListView
                x:Name="DeviceGroupsListView"
                IsItemClickEnabled="False"
                SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Spacing="10" Margin="0,0,0,20">
                            <StackPanel Spacing="2">
                                <TextBlock FontSize="22" FontWeight="SemiBold" Text="{Binding Name}" />
                                <TextBlock Text="{Binding Summary}" />
                            </StackPanel>

                            <GridView
                                ItemsSource="{Binding Devices}"
                                IsItemClickEnabled="False"
                                SelectionMode="None">
                                <GridView.ItemTemplate>
                                    <DataTemplate>
                                        <Border
                                            Tag="{Binding}"
                                            Tapped="DeviceCard_Tapped"
                                            Width="260"
                                            MinHeight="142"
                                            Padding="16"
                                            Background="{Binding CardBackground}"
                                            BorderBrush="{Binding CardBorderBrush}"
                                            BorderThickness="{Binding CardBorderThickness}"
                                            CornerRadius="8"
                                            Opacity="{Binding CardOpacity}">
                                            <FlyoutBase.AttachedFlyout>
                                                <Flyout Placement="Right">
                                                    <StackPanel Width="300" Spacing="14">
                                                        <StackPanel Spacing="2">
                                                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding Name}" TextWrapping="Wrap" />
                                                            <TextBlock Text="{Binding ControlSummary}" TextWrapping="Wrap" />
                                                        </StackPanel>

                                                        <ToggleSwitch
                                                            Header="Power"
                                                            IsEnabled="{Binding IsEnabled}"
                                                            IsOn="{Binding IsOn, Mode=OneWay}"
                                                            OffContent="Off"
                                                            OnContent="On"
                                                            Tag="{Binding}"
                                                            Toggled="PowerToggleSwitch_Toggled" />

                                                        <StackPanel Spacing="6" Visibility="{Binding BrightnessVisibility}">
                                                            <TextBlock Text="{Binding BrightnessLabel}" />
                                                            <Slider
                                                                Maximum="100"
                                                                Minimum="1"
                                                                StepFrequency="1"
                                                                Value="{Binding BrightnessPercent, Mode=TwoWay}" />
                                                        </StackPanel>

                                                        <StackPanel Spacing="6" Visibility="{Binding ColorTemperatureVisibility}">
                                                            <TextBlock Text="{Binding ColorTemperatureLabel}" />
                                                            <Slider
                                                                Maximum="{Binding MaxColorTemperatureKelvin}"
                                                                Minimum="{Binding MinColorTemperatureKelvin}"
                                                                StepFrequency="50"
                                                                Value="{Binding ColorTemperatureKelvin, Mode=TwoWay}" />
                                                        </StackPanel>

                                                        <StackPanel Spacing="6" Visibility="{Binding ColorVisibility}">
                                                            <TextBlock Text="Color" />
                                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                                <Button Width="32" Height="32" Padding="0" Background="#FFFFA05A" BorderThickness="1" Click="SetLightColorButton_Click" CommandParameter="{Binding}" IsEnabled="{Binding IsEnabled}" Tag="255,160,90" />
                                                                <Button Width="32" Height="32" Padding="0" Background="#FFFFD666" BorderThickness="1" Click="SetLightColorButton_Click" CommandParameter="{Binding}" IsEnabled="{Binding IsEnabled}" Tag="255,214,102" />
                                                                <Button Width="32" Height="32" Padding="0" Background="#FF5EEAD4" BorderThickness="1" Click="SetLightColorButton_Click" CommandParameter="{Binding}" IsEnabled="{Binding IsEnabled}" Tag="94,234,212" />
                                                                <Button Width="32" Height="32" Padding="0" Background="#FF60A5FA" BorderThickness="1" Click="SetLightColorButton_Click" CommandParameter="{Binding}" IsEnabled="{Binding IsEnabled}" Tag="96,165,250" />
                                                                <Button Width="32" Height="32" Padding="0" Background="#FFA78BFA" BorderThickness="1" Click="SetLightColorButton_Click" CommandParameter="{Binding}" IsEnabled="{Binding IsEnabled}" Tag="167,139,250" />
                                                            </StackPanel>
                                                        </StackPanel>

                                                        <Button
                                                            Click="ApplyLightControlsButton_Click"
                                                            CommandParameter="{Binding}"
                                                            IsEnabled="{Binding IsEnabled}"
                                                            Visibility="{Binding LightControlsVisibility}">
                                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                                <ProgressRing
                                                                    Width="16"
                                                                    Height="16"
                                                                    IsActive="{Binding IsBusy}"
                                                                    Visibility="{Binding BusyVisibility}" />
                                                                <TextBlock Text="Apply" />
                                                            </StackPanel>
                                                        </Button>
                                                    </StackPanel>
                                                </Flyout>
                                            </FlyoutBase.AttachedFlyout>
                                            <StackPanel Spacing="10">
                                                <Grid ColumnSpacing="10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="24" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <Button
                                                        Width="32"
                                                        Height="32"
                                                        Margin="-6,-3,0,0"
                                                        Padding="0"
                                                        Click="ToggleDeviceButton_Click"
                                                        CommandParameter="{Binding}"
                                                        IsEnabled="{Binding IsEnabled}"
                                                        ToolTipService.ToolTip="{Binding ToggleToolTip}">
                                                        <Grid>
                                                            <FontIcon FontSize="16" Glyph="{Binding IconGlyph}" Visibility="{Binding IconVisibility}" />
                                                            <ProgressRing
                                                                Width="16"
                                                                Height="16"
                                                                IsActive="{Binding IsBusy}"
                                                                Visibility="{Binding BusyVisibility}" />
                                                        </Grid>
                                                    </Button>
                                                    <StackPanel Grid.Column="1" Spacing="2">
                                                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding Name}" TextWrapping="Wrap" />
                                                        <TextBlock Text="{Binding Detail}" TextWrapping="Wrap" />
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4">
                                                        <Button
                                                            Width="32"
                                                            Height="32"
                                                            Padding="0"
                                                            Click="MoreControlsButton_Click"
                                                            IsEnabled="{Binding IsEnabled}"
                                                            ToolTipService.ToolTip="More controls"
                                                            Visibility="{Binding DetailControlsVisibility}">
                                                            <FontIcon FontSize="14" Glyph="&#xE712;" />
                                                        </Button>
                                                    </StackPanel>
                                                </Grid>

                                                <ItemsControl ItemsSource="{Binding StatusChips}">
                                                    <ItemsControl.HorizontalAlignment>Left</ItemsControl.HorizontalAlignment>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border
                                                                Margin="0,0,6,6"
                                                                Padding="8,4"
                                                                Background="{Binding Background}"
                                                                CornerRadius="8"
                                                                ToolTipService.ToolTip="{Binding ToolTip}">
                                                                <TextBlock FontSize="12" FontWeight="SemiBold" Foreground="{Binding Foreground}" Text="{Binding Text}" />
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <StackPanel Orientation="Horizontal" />
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
    </Grid>
</Page>
